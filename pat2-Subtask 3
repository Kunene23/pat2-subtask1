#include <iostream> 
#include <iomanip>   // Error 1: Missing header for setprecision 
 
using namespace std; 
 
const int NUM_EXPERIMENTS = 4;   // Error 2: Should be 4 experiments, not 
3 
const int NUM_READINGS = 3; 
 
int main() {   // Error 3: main must return int, not float 
    // Error 4: i and j should be int, not char 
    for (int i = 1; i <= NUM_EXPERIMENTS; i++) { 
        double total = 0;   // Error 5: Moved declaration inside loop 
        cout << "EXPERIMENT " << i << endl; 
        cout << "=============" << endl; 
 
        for (int j = 1; j <= NUM_READINGS; j++) { 
            double readingValue;   // Error 6: Declare readingValue 
inside loop 
            cout << "Enter reading " << j << " value: "; 
            cin >> readingValue;   // Error 7: Variable name was 
'reading' instead of 'readingValue' 
            total = total + readingValue;   // Error 8: Missing semicolon 
        } 
 
        double average = total / NUM_READINGS;   // Error 9: Remove "+ 
total" (wrong calculation) 
 
        // Error 10: Correct the logic for range checking 
        if (average < 100) { 
            cout << "Experiment " << i << " average: " 
                 << fixed << setprecision(2) << average 
                 << " is Below acceptable range\n"; 
        } else if (average >= 100 && average <= 300) {   // Error 11: 
'&&' not 'OR' 
            cout << "Experiment " << i << " average: " 
                 << fixed << setprecision(2) << average 
                 << " is Within acceptable range\n"; 
        } else if (average > 300) { 
            cout << "Experiment " << i << " average: " 
                 << fixed << setprecision(2) << average 
                 << " is Above acceptable range\n"; 
        } 
    } 
 
    return 0; 
}
